'use client';

import { withClasses } from '@spores/frontend/utils';
import { Header as H } from './header';

import classes from './header.module.scss';

export const Header = withClasses(classes, H);